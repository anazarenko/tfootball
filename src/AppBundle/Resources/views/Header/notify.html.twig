<li class="dropdown game-notify {% if confirms|length > 0 %}notify-active{% endif %}">
    <a aria-expanded="false" class="dropdown-toggle" data-toggle="dropdown" href="#">
        <i class="fa fa-bell fa-fw"></i>  <i class="fa fa-caret-down"></i>
    </a>
    <ul class="dropdown-menu dropdown-alerts match-confirm-list">
        {{ include('@App/Header/notify_item.html.twig', {'confirms' : confirms}) }}
    </ul>
    <!-- /.dropdown-alerts -->
</li>