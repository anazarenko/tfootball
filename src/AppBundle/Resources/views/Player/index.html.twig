{% extends 'AppBundle::base.html.twig' %}

{% block title %} - Players{% endblock %}

{% block content %}
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Username</th>
                <th title="Played">Pld</th>
                {#<th title="Won">W</th>#}
                {#<th title="Drawn">D</th>#}
                {#<th title="Lost">L</th>#}
                {#<th>%</th>#}
                {#<th title="Goal for">GF</th>#}
                {#<th title="Goal against">GA</th>#}
                {#<th title="Goal difference">GD</th>#}
            </tr>
        </thead>
        <tbody>
            {% for team in teams %}
                <tr>
                    <td>{{ team|members }}</td>
                    <td>{{ team.games|length }}</td>
                    {#<td>{{ player.wonGames(1)|length }}</td>#}
                    {#<td>{{ player.drawnGames(1)|length }}</td>#}
                    {#<td>{{ player.lostGames(1)|length }}</td>#}
                    {#<td>#}
                        {#{% if player.games(1)|length > 0 %}#}
                            {#{{ ((player.wonGames(1)|length/player.games(1)|length)*100)|number_format(1, '.', ',') }}#}
                        {#{% endif %}#}
                    {#</td>#}
                    {#<td>0</td>#}
                    {#<td>0</td>#}
                    {#<td>0</td>#}
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
